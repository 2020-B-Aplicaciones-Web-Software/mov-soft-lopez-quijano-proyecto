package com.example.proyecto_segundo_bimestre_lopez_quijano

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.ArrayAdapter
import android.widget.ListView

class AgregarLista : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_crear_lista)

        // TODO: Implementar esta actividad

        /*
        val arrelo = arrayListOf<String>("a","b", "c","d","e")

        val adaptador = ArrayAdapter(
            this,
            android.R.layout.simple_list_item_1,
            arrelo
        )

        val list = findViewById<ListView>(R.id.lv_miembrosListaCrear)
        list.adapter = adaptador
        */
    }
}